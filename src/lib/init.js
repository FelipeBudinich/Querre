// -----------------------------------------------------------------------------
// ini Namespace (initialization)

(function (window) {
    'use strict';
    window.ini = {
        initApplication: function () {
            // Register keys events
            kb.registerKeysEvents();

        }
    };

})(window);